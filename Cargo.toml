[package]
name = "paired"

# Remember to change version string in README.md.
version = "0.16.1"
authors = [
    "Sean Bowe <ewillbefull@gmail.com>",
    "Jack Grigg <jack@z.cash>",
]
readme = "README.md"
license = "MIT/Apache-2.0"

description = "Pairing-friendly elliptic curve library"
documentation = "https://docs.rs/paired/"
homepage = "https://github.com/filecoin-project/paired"
repository = "https://github.com/filecoin-project/paired"
edition ="2018"

[dependencies]
byteorder = "1"
blake2b_simd = "0.5"
serde = { version = "1.0", optional = true, features = ["derive"] }
ff = { version = "=0.5.0", features = ["derive"] }
groupy = { version = "0.2.1" }
rand_core = "0.5"

[dev-dependencies]
rand_xorshift = "0.2"
serde_json = "1.0"

[features]
unstable-features = ["expose-arith"]
expose-arith = []
default = []

[badges]
maintenance = { status = "actively-developed" }

[patch.crates-io]
ff = { git = "https://github.com/filecoin-project/ff", branch = "asm" }